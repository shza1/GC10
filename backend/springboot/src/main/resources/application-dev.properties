# Development Database (Local MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce_dev?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Jordan is the GOAT23!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Secret (not secure - only for development)
jwt.secret=dev-secret-key-minimum-256-bits-long-not-for-production

# Logging
logging.level.root=INFO
logging.level.com.yourcompany.ecommerce=DEBUG
logging.level.org.springframework.security=DEBUG